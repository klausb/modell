[blender]
host = "127.0.0.1"
port = 8765
token = "change-me"
timeout_seconds = 30
connect_retries = 2
retry_backoff_seconds = 0.5

[llm]
provider = "openai_compatible"
model = "some-model"
base_url = "https://model-provider.com/v1"
api_key = "change-me"
temperature = 0.2
max_tokens = 2000

[agent]
max_steps = 10
verbosity = 1
planning_interval = 1
require_confirmation_for_destructive = true